# PostgreSQL Configuration
# Generate a strong password for your database
POSTGRES_PASSWORD=your_secure_password_here

# Session Configuration  
# Generate a random secret: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here

# Optional: If you need to connect to an external database instead of the Docker one
# DATABASE_URL=postgresql://user:password@host:5432/database

# ======================
# DEPLOYMENT COMMANDS
# ======================
# After starting containers:
# 1. Run database migrations:
#    docker compose exec app npm run db:push
#
# 2. Seed the database with initial data:
#    docker compose exec app npx tsx scripts/seed_data.ts

# ======================
# POCKET ID & AUTH PROXY
# ======================
POCKET_ID_CLIENT_ID=
POCKET_ID_CLIENT_SECRET=


# ======================
# CADDY / DOMAIN CONFIG
# ======================
DOMAIN_NAME=mitre.localhost
AUTH_DOMAIN=auth.localhost
ACME_EMAIL=admin@localhost


# ======================
# TROUBLESHOOTING NOTES
# ======================
# If you get 'Connection Refused' on login redirect:
# 1. Ensure POCKET_ID_CLIENT_ID and POCKET_ID_CLIENT_SECRET in .env match exactly what is in PocketID.
# 2. Ensure the OIDC Client Redirect URI in PocketID is EXACTLY: https://opentidal.rivia.com.au/oauth2/callback
# 3. Check if Caddy is successfully routing HTTPS to PocketID (https://auth.rivia.com.au should load).

# Pangolin Configuration
ENCRYPTION_KEY=
JWT_SECRET=

# ======================
# AI PROVIDERS
# ======================
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
